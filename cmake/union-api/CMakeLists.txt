if(${BUILD_UNION_API_STATIC})
    set(UNION_API_BUILD_TARGET UnionAPIStatic)
    set(UNION_API_BUILD_TYPE STATIC)
    include(union-api.cmake)
    list(APPEND UNION_API_TARGETS UnionAPIStatic)
    set(UNION_API_TARGETS ${UNION_API_TARGETS} PARENT_SCOPE)
endif()

if(${BUILD_UNION_API_SHARED})
    set(UNION_API_BUILD_TARGET UnionAPI)
    set(UNION_API_BUILD_TYPE SHARED)
    include(union-api.cmake)
    list(APPEND UNION_API_TARGETS UnionAPI)
    set(UNION_API_TARGETS ${UNION_API_TARGETS} PARENT_SCOPE)
endif()